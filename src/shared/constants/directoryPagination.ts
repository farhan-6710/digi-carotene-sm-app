/** How many DirectoryTable rows to show before infinite-scroll loads the next chunk. */
export const DIRECTORY_PAGE_SIZE = 30;

/** Brief pause so the bottom loader is visible when appending the next window. */
export const DIRECTORY_LOAD_MORE_DELAY_MS = 120;
