<?php

declare(strict_types=1);

/**
 * @deprecated Use sync_yesterday_ad_acc.php (same cron job). Kept so Hostinger
 * URLs that still point at the old filename keep working.
 */
require __DIR__ . '/sync_yesterday_ad_acc.php';
